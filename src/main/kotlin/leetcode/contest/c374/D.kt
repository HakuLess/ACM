package leetcode.contest.c374

import utils.print

fun main() {
    val s = SolutionD()
    // 4
    s.numberOfSequence(5, intArrayOf(0, 4)).print()
    // 3
    s.numberOfSequence(4, intArrayOf(1)).print()
}

class SolutionD {
    fun numberOfSequence(n: Int, sick: IntArray): Int {
        val mod = 1000000007L
        return 0
    }
}