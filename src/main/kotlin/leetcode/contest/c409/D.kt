package leetcode.contest.c409

import utils.print
import utils.toGrid

fun main() {
    val s = SolutionD()
    s.numberOfAlternatingGroups(intArrayOf(0, 1, 1, 0, 1), "[[2,1,0],[1,4]]".toGrid()).joinToString().print()
}

class SolutionD {
    fun numberOfAlternatingGroups(colors: IntArray, queries: Array<IntArray>): List<Int> {

        return listOf()
    }
}